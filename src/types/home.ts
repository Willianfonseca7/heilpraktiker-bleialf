export type FeatureCardItem = {
  title: string;
  image: string;
  items: string[];
  href?: string;
  buttonLabel?: string;
  imageObjectPosition?: string;
};

export type HeroSlide = {
  key: string;
  label: string;
  image: string;
};
